//6. Given five numbers as input. Calculate and print the average of the numbers(without using arrays).

let a = 45, b = -12, c = 0, d = 3, e = -15;
let mijin = 0;
mijin = (a + b + c + d + e) / 5;
alert(`average: ${mijin}`);

let a1 = 7, b1 = 52, c1 = -23, d1 =  9, e1 = -81;
mijin1 = (a1 + b1 + c1 + d1 + e1) / 5;
alert(`average: ${mijin1}`);